{
  "name": "camp-codex-skill-tree",
  "private": true,
  "description": "Verification tooling for the Camp Codex builder skill tree page. The site itself (index.html) has zero dependencies.",
  "scripts": {
    "verify": "node scripts/verify.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
